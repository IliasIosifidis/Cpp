#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int powResult = pow(2,10);
    int sqrtResult = sqrt(16);
    int ceilResult = ceil(18.2);
    int floorResult = floor(18.2);
    int logResult = log2(512);

    cout << powResult << endl;
    cout << sqrtResult << endl;
    cout << ceilResult << endl;
    cout << floorResult << endl;
    cout << logResult << endl;
}